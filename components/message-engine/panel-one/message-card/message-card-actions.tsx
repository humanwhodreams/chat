import { ChevronDown } from "lucide-react";

type Props = {};

const MessageCardActions = (props: Props) => {
  return (
    <>
      <ChevronDown size={20} className="text-muted-foreground" />
    </>
  );
};

export default MessageCardActions;
